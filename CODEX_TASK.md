# Codex 任務：完成韓國釜山自由行保險線上系統

請在此 repository 直接完成可部署版本，不要改動 `AGENTS.md` 定義的固定保險內容。

## 目標
把目前靜態前端 + Supabase Edge Function 原型完成為可多人同時使用的正式線上系統。

## 必做項目
1. 檢查並修正目前所有前後端錯誤，確保 `index.html`、`config.js`、`assets/`、`supabase/` 可正常協作。
2. 完成 Supabase schema 與 Edge Function 部署相容性。
3. 公司登入必須由伺服器驗證，前端不可包含密碼。
4. 統計表必須使用獨立密碼，只有驗證成功才回傳解密資料。
5. 一個人只能有一個方案；同一身分證再次送出時 upsert 更新。
6. 年齡以 2026-10-18 計算：未滿 15 歲只能 Y1；已滿 15 歲只能 U3/Z10/Z9。
7. 伺服器端再次驗證方案、保額、保費，不接受前端竄改數字。
8. 敏感個資寫入資料庫前用 AES-256-GCM（32 byte key）加密。
9. 身分證只保存 SHA-256 雜湊索引 + 加密 payload，不保存明文。
10. 增加伺服器端基本 rate limiting / 失敗登入防暴力機制；若 Supabase Edge Function 不適合永久計數，可使用合理且可部署的替代方式並說明。
11. 清除全部統計資料必須再次驗證統計密碼，並加入明確二次確認。
12. 手機版操作良好；長表格可橫向捲動；原始圖片可放大查看。
13. 原始保險圖片與保險數字不得修改。
14. 加上健康檢查或明確的 API 設定錯誤提示，避免前端只顯示不明錯誤。
15. 提供最簡單的正式部署方式（Supabase + 任一靜態 hosting），不要把 secrets commit 到 repo。

## Secrets（只設在 Supabase / Codex Environment，不可 commit）
- `COMPANY_ACCESS_CODE`
- `STATS_PASSWORD`
- `DATA_ENCRYPTION_KEY`
- Supabase 平台提供的 URL / service role secret

使用者已另行指定實際公司密碼與統計表密碼；請只透過環境變數設定，不要把值寫入任何 tracked file、commit、log 或 README。

## 驗收測試
請至少驗證以下情境：
- 錯誤公司密碼無法登入。
- 正確公司密碼登入後可操作。
- 2011-10-19 出生者在 2026-10-18 未滿 15 歲，只能 Y1。
- 2011-10-18 出生者在 2026-10-18 剛滿 15 歲，不可 Y1，可選成人方案。
- 未滿 15 歲嘗試由 API 送 U3/Z9/Z10 被伺服器拒絕。
- 滿 15 歲嘗試由 API 送 Y1 被伺服器拒絕。
- 竄改 premium / planName / cover 被伺服器拒絕。
- 同一身分證連續送兩次，統計表只有一筆且為最新方案。
- 錯誤統計密碼不可取得資料。
- 正確統計密碼可看到總人數、Y1 人數、成人方案人數、總保費與逐筆資料。
- 清除資料需要正確統計密碼且有二次確認。
- 資料庫中看不到姓名、身分證、生日的明文。

## 完成時輸出
- 修改摘要
- 測試結果
- Supabase 部署指令
- 必填 secrets 清單（只列名稱，不列值）
- 靜態網站部署步驟
- 若仍有任何手動步驟，逐項列出
